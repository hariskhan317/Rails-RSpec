<%= form_with(model: url_list) do |form| %>
  <% if url_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(url_list.errors.count, "error") %> prohibited this url_list from being saved:</h2>

      <ul>
        <% url_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
